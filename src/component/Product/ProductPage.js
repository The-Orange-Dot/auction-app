import React from "react";

const ProductPage = ({ children }) => {
  //This component acts as a container for category navigator, searchbar, and product cards
  return <div>{children}</div>;
};

export default ProductPage;
