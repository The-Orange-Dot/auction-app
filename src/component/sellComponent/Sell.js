import React from "react";

const Sell = () => {
  return (
    <span className="sell-container" onClick={() => console.log("SOY! SOY!")}>
      <p className="sell">START SELLING</p>
    </span>
  );
};

export default Sell;
